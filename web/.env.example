# Base URL of the API, no trailing slash.
# Local dev points at the docker-compose API; in production set this in the
# Vercel project settings to the deployed Railway URL.
VITE_API_URL=http://localhost:8000
